meta {
  name: create-notifications
}

script:post-response {
  let responseBody = res.body;
  bru.setVar("mostRecentNotificationOrderId", responseBody.notificationOrderId);
  bru.setVar("mostRecentShipmentId", responseBody?.notification?.shipmentId);
}
