meta {
  name: get by dialog id
  type: http
  seq: 1
}

get {
  url: {{host}}/notifications/api/v1/future/log?dialogId={{dialogporten_dialog_id}}
  body: none
  auth: inherit
}

params:query {
  dialogId: {{dialogporten_dialog_id}}
}
