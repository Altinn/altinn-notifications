meta {
  name: Get Daily Email metrics
  type: http
  seq: 2
}

post {
  url: {{host}}/notifications/api/v1/metrics/email
  body: none
  auth: inherit
}

headers {
  X-API-Key: {{process.env.METRICS_API_KEY}}
  Ocp-Apim-Subscription-Key: {{process.env.METRICS_SUBSCRIPTION_KEY}}
}

settings {
  encodeUrl: true
  timeout: 0
}
