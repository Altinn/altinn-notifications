meta {
  name: 01-create
  type: http
  seq: 1
}

post {
  url: {{host}}/notifications/api/v1/future/orders/instant/sms
  body: json
  auth: inherit
}

body:json {
  {
    "idempotencyId": "{{random_idempotency_id}}",
    "sendersReference": "e2e-smsfail-{{senders_reference_guid}}",
    "recipientSms": {
      "phoneNumber": "+4741000001",
      "timeToLiveInSeconds": 120,
      "smsSettings": {
        "sender": "E2ETest",
        "body": "(E2E) SMS to invalid recipient mock number"
      }
    }
  }
}

script:post-response {
  if (res.status === 201 && res.body && res.body.notification && res.body.notification.shipmentId) {
    bru.setVar("e2eSmsFailShipmentId", res.body.notification.shipmentId);
  }
}

tests {
  test("returns 201 Created (order accepted, failure happens async)", function() {
    expect(res.status).to.equal(201);
  });

  test("response has shipmentId", function() {
    expect(res.body.notification).to.have.property("shipmentId");
  });
}
