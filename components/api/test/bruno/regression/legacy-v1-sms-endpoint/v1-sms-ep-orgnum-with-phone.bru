meta {
  name: v1-sms-ep-orgnum-with-phone
  type: http
  seq: 6
}

post {
  url: {{host}}/notifications/api/v1/orders/sms
  body: json
  auth: inherit
}

body:json {
  {
    "resourceId": "app_ttd_regression-test",
    "body": "(REGRESSION) Orgnum with phone test",
    "recipients": [
      {
        "mobileNumber": "+4799999999",
        "organizationNumber": "123456789"
      }
    ]
  }
}

tests {
  test("returns 400 Bad Request", function() {
    expect(res.status).to.equal(400);
  });

  test("error mentions combination not allowed", function() {
    const body = JSON.stringify(res.body).toLowerCase();
    expect(body).to.include("combin");
  });
}
