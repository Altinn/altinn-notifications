meta {
  name: reminder-neither-delay-nor-sendtime
  type: http
  seq: 4
}

post {
  url: {{host}}/notifications/api/v1/future/orders
  body: json
  auth: inherit
}

body:json {
  {
    "idempotencyId": "{{random_idempotency_id}}",
    "sendersReference": "regression-reminder-{{senders_reference_guid}}",
    "recipient": {
      "recipientEmail": {
        "emailAddress": "{{process.env.TESTDATA_MY_EMAIL}}",
        "emailSettings": {
          "subject": "(REGRESSION) Reminder Test",
          "body": "(REGRESSION) Reminder test - {{random_idempotency_id}}",
          "contentType": "Plain",
          "sendingTimePolicy": "Anytime"
        }
      }
    },
    "reminders": [
      {
        "sendersReference": "regression-rem-4",
        "recipient": {
          "recipientEmail": {
            "emailAddress": "{{process.env.TESTDATA_MY_EMAIL}}",
            "emailSettings": {
              "subject": "(REGRESSION) Reminder",
              "body": "(REGRESSION) Reminder body",
              "contentType": "Plain",
              "sendingTimePolicy": "Anytime"
            }
          }
        }
      }
    ]
  }
}

tests {
  test("should return 400 when neither delayDays nor requestedSendTime is provided", function() {
    expect(res.status).to.equal(400);
  });

  test("response should mention DelayDays or RequestedSendTime requirement", function() {
    const body = JSON.stringify(res.body).toLowerCase();
    const mentionsRequirement = body.includes("delaydays") || body.includes("requestedsendtime");
    expect(mentionsRequirement).to.be.true;
  });
}
