meta {
  name: v1-get-email-notifications
  type: http
  seq: 7
}

get {
  url: {{host}}/notifications/api/v1/orders/:notificationId/notifications/email
  body: none
  auth: inherit
}

params:path {
  notificationId: {{v1OrderId}}
}

tests {
  test("returns 200 OK", function() {
    expect(res.status).to.equal(200);
  });
}
